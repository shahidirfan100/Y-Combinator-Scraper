{
  "title": "Y Combinator Companies Scraper Input",
  "description": "Configure the scraper to extract company data from Y Combinator directory",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "Start URL",
      "type": "string",
      "description": "URL to scrape companies from (e.g., https://www.ycombinator.com/companies?batch=W25)",
      "editor": "textfield",
      "example": "https://www.ycombinator.com/companies"
    },
    "scrape_all_companies": {
      "title": "Scrape all companies",
      "type": "boolean",
      "description": "Scrape all companies from the directory. If false, use the provided URL or default page.",
      "editor": "checkbox",
      "default": false
    },
    "scrape_founders": {
      "title": "Scrape founders data",
      "type": "boolean",
      "description": "Extract founder details from company profiles (increases runtime)",
      "editor": "checkbox",
      "default": false
    },
    "scrape_open_jobs": {
      "title": "Scrape job openings",
      "type": "boolean",
      "description": "Extract open job positions from company profiles (increases runtime)",
      "editor": "checkbox",
      "default": false
    },
    "results_wanted": {
      "title": "Results limit",
      "type": "integer",
      "description": "Maximum number of companies to scrape",
      "minimum": 1,
      "maximum": 10000,
      "default": 20,
      "editor": "number",
      "prefill": 20
    },
    "max_pages": {
      "title": "Maximum pages",
      "type": "integer",
      "description": "Safety limit on number of pages to scrape",
      "minimum": 1,
      "maximum": 100,
      "editor": "number",
      "default": 5,
      "prefill": 5
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Proxy settings for reliable data extraction",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
